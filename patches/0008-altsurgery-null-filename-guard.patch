diff --git a/SimPE.AltSurgery/SurgeryTool.cs b/SimPE.AltSurgery/SurgeryTool.cs
index b28ecbd..6add96e 100644
--- a/SimPE.AltSurgery/SurgeryTool.cs
+++ b/SimPE.AltSurgery/SurgeryTool.cs
@@ -52,9 +52,10 @@ namespace SimPe.Plugin
 		public bool IsEnabled(SimPe.Interfaces.Files.IPackedFileDescriptor pfd, SimPe.Interfaces.Files.IPackageFile package)
 		{
 			if (package==null) return false;
-			if (prov.SimNameProvider == null) return false;
+			if (package.FileName == null) return false;   // guard: IsNeighborhoodFile NREs on a null filename (matches built-in Sims Surgery's REQUIRED FIX)
+			if (prov == null || prov.SimNameProvider == null) return false;
 			return Helper.IsNeighborhoodFile(package.FileName);
-			
+
 		}
 
 		Surgery surg;
